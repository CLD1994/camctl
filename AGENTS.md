# AGENTS.md

根目录的 AGENTS.md 只写最高层级的硬性约束和方向性原则，不写具体操作细节。

## Working Style

- 将自己放在和人类平等的位置上，不要迎合他人。

- 要独立判断，勇于说“不”，技术辩论是是可以畅所欲言的。

- 先读上下文，再行动。

- 优先使用中文交流，但保留原文中的准确名称和标识符。对于其他技术术语，如果有自然、通行的中文译法，就使用中文。

- 如果英文有助于消除歧义或已经在中文圈广为人知，就保留英文, 不要为了避免使用英文而生造生硬的直译词。必须使用英文术语时，如果上下文中没有出现过，则应紧跟括号说明其中文含义。

- 使用外部模板或英文参考资料时，只继承必要的信息结构和约束含义，不照搬其语言；项目文档的标题、说明和步骤仍应以中文为主体，代码、命令、字段名、协议字面量和必要专有名词除外。

- 使用熟悉、具体的语言。用证据支持的具体行为来替代俚语或模糊判断。如果来源没有说明究竟发生了什么变化，就明确说明其含义不清楚，不要自行推测或编造机制。

- 命名应结合所在上下文：上下文已经明确对象时省略重复限定；跨对象或混合上下文时补全限定，使名称既简洁又不产生歧义。

- 解释技术问题或编写规范时，优先使用具体对象和动作，避免连续堆叠抽象名词。规则应尽量说明适用场景、错误做法、错误原因和正确做法，使不了解前置背景的人也能直接理解和执行。

- 如果项目发生了改变从而使根目录的 AGENTS.md 或作用域内的 AGENTS.md 收到影响, 则必须更新它们.

- 最终产物只表达当前有效的目标状态，不表达它是如何从过去的错误状态修正而来的。用户的否决、纠正和负向约束，默认都是用于更新内部目标模型的控制信息，而不是最终产物的内容。一旦某项方案被否决，就应当从当前方案中彻底消失；不要通过标题、注释、说明、命名、提交信息或其他形式继续保留它的痕迹。

- 用正确的逻辑覆盖错误而不是打补丁

- 未完成 skill 正文及必需参考文件的实际读取前，不得把摘要、历史记录或“已加载”声明视为有效加载。

- 系统级和开发者级指令，skill、参考文件、AGENTS.md 或用户要求之间出现冲突时，必须停止相关动作，列出冲突和优先级，不得静默选择。

- 本仓库的工作区只由用户、当前 Agent 与其明确指派的 subagent 共同修改。执行期间出现新改动时，视为已知协作者的工作并结合当前状态继续，不将未知第三方并发写入作为防御前提。

## Environment

- 通过环境变量 `DEVCONTAINER=true` 判断当前为容器开发环境。容器环境通常预装 uv 和 pnpm 作为依赖管理工具，应优先使用。
- 执行 Python 脚本时，优先使用 uv 创建的非项目级虚拟环境，而非系统 Python。该环境通常已创建于 `~/.venv`, 如果没有就创建它。

## 测试规范

- 如果遇到测试失败或静态检查失败，先定位根因，再修复。不要用删除测试、放宽配置、吞掉错误的方式让门禁变绿。

- 项目测试的顶层分类是单元测试和集成测试。单元测试验证局部调用逻辑与行为契约，集成测试验证真实协作者组合后的运行逻辑；其他执行属性不得模糊二者的归属。

- 单元测试默认以一个具有独立语义契约的生产函数为被测单元，一个测试用例只验证该函数的一个行为分支。“一个函数一个功能”是对生产设计的单一职责要求，不等于每个私有辅助函数或简单转发函数都必须拥有独立测试；没有独立契约的实现细节应通过公开函数验证。

- 采用偏模拟派的测试策略。测试替身（test double）是主动隔离协作者、控制输入与故障、缩小失败范围的常规设计工具，不以“迫不得已”为使用前提。根据目的选择 `stub`、`fake`、`mock` 或 `spy`。

- 单元测试应优先断言被测函数的返回值、异常和它所拥有的状态变化。只有调用是否发生、调用次数、参数或顺序本身属于协议、安全或副作用契约时，才对 `mock` 或 `spy` 的交互进行断言；不要把内部函数调用图复制成测试预期。

- 测试替身应隔离稳定的协作者边界，而不是机械替换被测函数调用的每个内部函数。仅改变内部实现而不改变行为契约时，相关测试原则上不应大范围修改；如果大量交互断言随重构一起变化，应审计测试是否已与实现细节耦合。

- 测试替身必须受真实接口约束，并覆盖合法返回、错误和边界状态，避免替身行为与真实实现漂移。凡是被替身隔离的重要协作关系，都必须有相应集成测试验证真实双方可以共同工作。

- 单元测试应快速、确定且在同一进程内运行；除解释器导入被测源码外，不访问真实文件系统、网络、数据库或用户级全局状态，不启动子进程，也不执行包构建、安装或解析。涉及这些真实边界、完整分发物、工具缓存或多进程并发的测试属于集成测试；集成测试可以耗时较长，但必须与单元测试分开执行并保留明确的失败诊断。

- 测试应从行为契约和状态分类独立推导，而不是复制实现逻辑。根据风险覆盖合法、非法、边界和状态转换；测试预期不得依赖被测代码中的同一计算路径。开发时优先用能够证伪契约的最窄单元测试完成红—绿—重构循环，只有跨边界行为无法在单元层证明时才从集成测试开始。

- 测试必须区分机器使用的数据和人类阅读的文案。错误码、字段名、退出码等机器协议应精确断言；可翻译或仅供阅读的文案，默认验证输入事实是否正确呈现，以及输出的结构、顺序、数量和特殊字符处理是否正确，不应逐字比较，也不应在多个测试层重复验证同一文案。只有需求明确要求某段文案逐字不变时，才在最接近用户输出的一层测试中精确断言一次。

- 验证模块之间的依赖限制时，不要只搜索源码中有没有某段文字，因为注释、换行、别名等都可能造成误判；应直接检查实际的导入关系、代码结构或运行行为。

## 规格与计划规范

- 写具体的代码实现之前，先把需求重述为可验证的行为契约：输入、输出、不变量、允许的状态转换和失败语义。无法明确契约时，先澄清，不凭实现便利自行选择语义。

- 编写规格时，如果行为由多个独立条件共同决定，必须使用决策表、状态转换模型或等价的形式系统地定义状态空间，明确条件维度、组合约束以及每个有效分区的结果。示例只能说明规则，不能代替完整的分类和判定规则。

- 编写实施计划时，必须区分已经确认的行为契约、项目规则与对实现方式的预估。前两者可以作为硬性要求；模块划分、内部接口、预计修改文件和提交拆分等内容，如果没有被批准规格明确固定，应标为建议或预估，不得限制实施者根据实际数据流和代码结构作出必要调整。

- 编写和审查实施计划时，除逐项核对需求覆盖外，还必须对跨任务、跨组件和跨层的横切关注点进行端到端契约闭合审查，沿权威输入、派生过程、副作用、失败、重试、恢复直到可观察结果追踪数据流与控制流。各任务分别完整，不代表任务之间的接缝已经定义完整。

- 计划作为最终产物，目标阅读者并不仅仅是你自己，而是面向没有上下文的第三方执行者。所以不要带入只存在于你上下文中而他人无法理解的信息。

## 架构设计规范

- 状态数据库以不可变历史为权威来源，历史按具有独立含义的事实记录，在同一事务中同步更新当前状态投影；历史回放只重建状态，不执行外部副作用。数据库数据永久保留，当前不设计数据库数据删除机制；具体边界遵守[设计总览](docs/superpowers/specs/2026-09-08-camctl-cli-design.md#持久化与配置基础)。

- 对会导致不同行为的现实状态进行显式建模。不存在、无效、未知、未完成和有效状态不得无依据地折叠为同一个空值、默认值或跳过分支。

- 同一组需要长期保持一致的信息只能有一个权威来源。依赖列表、已登记错误码、状态和分支标识等，不得在配置、生产代码、脚本和测试中各自维护一套完整清单；其他地方应从权威来源读取或生成。

- 设计和评审安全性与可靠性时，不预设所有组件采用同一保障强度。应根据组件掌握的数据、可能产生的副作用、失败后果、部署环境和当前产品阶段评估风险，并主动与用户确认威胁模型、故障模型、当前保障范围及后置事项；当不同选择会显著改变实现复杂度或使用体验时，不得替用户默认决定。高风险组件可以采用严格保障，普通组件应遵循风险相称原则。程序用途或运行环境变化后，应重新讨论和调整这些边界。

## 评审规范

- 当进行对抗性审查时，不要尝试实现代码来对某个漏洞进行攻击，这会触发网络安全审查，导致当前对话直接中断。聚焦于防御性结果，例如识别、预防或修复安全问题。省略实现该结果并不必要的漏洞利用细节。

- 完成评审后，必须针对每个发现的问题分别给出可执行的修复建议，不得只报告现象，也不得合并不同责任边界的问题。每条建议至少说明问题及触发场景、根因与被违反的不变量、修复方案、影响范围、验证方式或回归测试；存在尚未核验的前提或未决事项时，必须明确列出。应区分已经确认的行为契约与对实现方式的建议，并要求修复后审计相关函数和模块中的同类风险。

- 向执行 Agent 交接评审缺陷时，不得假设执行者会自行补全省略的关键设计。评审者必须把已确认的行为契约、不变量、状态分类、失败与恢复语义、禁止采用的捷径、修复边界和可证伪的验收条件写成自包含指令；多个独立条件共同决定行为时，必须直接提供决策表、状态转换模型或等价的完整分类。发现同类问题跨入口存在时，必须列出已知入口和需要审计的责任边界，不能只笼统要求“检查同类问题”。

- 每轮评审结束后，评审者必须根据缺陷数量、相互依赖、影响范围和修复难度，决定是否在交接前编写执行计划。缺陷数量只是判断因素之一，不设固定数量阈值；只要修复涉及状态机、多个入口、持久化副作用、并发、失败恢复或跨组件契约，即使只有一个缺陷也应视为复杂修复。只有少量、局部、彼此独立且行为语义已经完全确定的修改，才可以不写执行计划而直接交接。

- 对无需计划的简单修复，逐项自包含的修复建议就是执行依据。对复杂修复，评审者必须先把缺陷整理成执行 Agent 可以机械落实的具体计划，明确任务顺序和依赖、每步必须保持的契约与不变量、需要覆盖的状态和入口、禁止采用的捷径、先失败测试后实现的步骤、阶段门禁以及最终验收条件；不得只给方向性原则，再要求执行 Agent 自行补全关键分析。

- 执行 Agent 必须按交接建议或执行计划逐项实施和汇报。发现计划与代码事实不一致、遇到计划未定义的状态、需要改变既定协议或无法满足验收条件时，应停止相关任务并报告具体证据，不得自行选择语义或用局部补丁绕过。评审者必须独立核验实际改动和验证证据，不能把执行 Agent 的完成声明当作完成依据。

- 后续评审必须区分修复引入的回归、已知问题未完整修复、原有潜伏缺陷和验收定义未闭合，并根据归因调整修复指导。相同不变量连续出现缺陷时，不得继续只给局部案例和原则性建议；必须补齐覆盖相关状态、分支、入口、失败、重试与恢复路径的模型和反例矩阵，明确指导执行 Agent 完成闭合修复。

- 评审必须把每轮完整结论分别写入当前工作区的 `tmp/agents/review-notes/<review-topic>/YYYY-MM-DD-round-<N>.md`；后续轮次开始前必须新建本轮文件，不得覆盖以前轮次。

## Implementation Discipline

- 修复缺陷时，不把评审或错误报告中的具体案例当作完整规格。应先识别被违反的不变量，再审计所有共享该不变量的状态、分支和入口，修复这一类问题，而不是只修复已展示的实例。

- 在修改代码前追踪相关数据流和控制流，包括生产者、消费者、正常路径、失败路径和恢复路径。优先在所有相关调用共同经过的最窄责任边界统一保证规则。

- 实现应直接表达协议或业务语义。不要用只适用于常见输入的启发式判断代替精确规则；不要为了修复当前问题进行无关重构、增加推测性抽象或引入不必要依赖。

- 对数量有限、会影响程序分支的字符串标识，应优先使用枚举（集中列出所有合法值的类型）或其他有约束的类型，避免到处手写；如果输入允许来自未来版本或外部系统，仍必须明确保留未知值的处理方式。

- 每个异常捕获、默认值、跳过、降级和清理分支都必须具有明确语义。无法证明安全的状态应停止并保留可诊断信息，不得静默解释为成功、不存在或无关。

- 不需要重复验证执行计划中明确已经验证过的信息，但是执行计划不保证绝对的正确性。在执行过程中发现冲突项需要明确列出。如果冲突项需要用户决策，则立刻停止执行并汇报。

- 存在尚未核验的前提或未决事项时必须明确列出。测试全绿不能替代对需求完整性的检查。
