# 请求受理与会话

[返回设计总览](README.md) · [阅读路线](reading-guide.md)

本专题与[CLI 命令](cli-commands.md)、[计划受理](plan-acceptance.md)、[取消计划与动作](task-cancellation.md)共同定义调用方、主程序与 camctl 的请求和进程协议。本页集中定义执行会话之间的工作交接，以及这些入口共同的持久化与验收要求。动作特有参数与执行结果由各动作专题定义；`last_report_id` 的有效性及覆盖含义由[状态报告与累计确认](status-reports.md)定义。

主程序由第三方维护，其集成职责是 CLI 进程管理和数据中转。进程协议应让它能够判断会话结束结果、处理后续启动，并尽量减少它需要实现的协调逻辑。计划受理及动作执行的业务结果由 camctl 向调用方客户端表达。

**阅读提示：** 本页重点是多个进程之间如何可靠交接工作。调用方式见[CLI 命令](cli-commands.md)，计划保存与重送见[计划受理](plan-acceptance.md)，取消目标见[取消计划与动作](task-cancellation.md)。本页末尾保留这些入口共同的持久化与验收要求。

本页导航：

- [会话接管协议](#会话接管协议)
- [责任与依据](#责任与依据)
- [两把锁及生命周期](#两把锁及生命周期)
- [待处理工作的判定](#待处理工作的判定)
- [`submit` 的事务顺序](#submit-的事务顺序)
- [`run` 正常关闭接纳](#run-正常关闭接纳)
- [失败与恢复边界](#失败与恢复边界)
- [新计划受理序列](#新计划受理序列)
- [持久化记录](#持久化记录)
- [验收要求](#验收要求)
- [实现时细化](#实现时细化)
- [主程序有限重启建议](#主程序有限重启建议)

## 会话接管协议

### 责任与依据

成功提交后仍需驱动的本地工作，必须由当前接纳者承担，或通过 `needs_run: true` 请求后续会话。请求受理、接纳资格观察与正常关闭接纳使用同一 SQLite 数据库的写事务确定先后顺序；协调全部由 camctl 完成。

SQLite 同时只允许一个写事务。协议使用 `BEGIN IMMEDIATE` 在检查状态前取得写事务资格，避免先读旧快照再尝试升级事务。事务忙等待及失败必须有有限预算，具体配置待细化。[SQLite 事务文档](https://sqlite.org/lang_transaction.html)

可以把两把锁分别理解为两个问题：“现在是否已有一个 `run` 进程负责执行？”以及“它现在是否还承诺接手新提交的工作？”进程准备退出时，前者仍然成立，后者已经结束。将这两个问题分开，`submit` 才能判断是否需要请主程序再启动一次 `run`。下面规定实际锁的持有和检查顺序。

### 两把锁及生命周期

同一状态库的所有 `run/submit` 入口使用同一对稳定锁文件。锁文件的存在只提供锁定对象；实际资格由内核锁持有情况表达。锁文件在正常运行和清理期间保持稳定，不通过删除重建来释放锁。

| 锁 | 持有者与持有期 | 责任 |
| --- | --- | --- |
| 会话锁 | `run` 在初始化前取得，覆盖初始化、接纳、收尾，直到退出清理 | 同一状态库最多一个执行会话 |
| 接纳锁 | `run` 在能够承担普通调度责任时取得，持有至关闭接纳 | 承诺检查并驱动接纳期间成功提交的工作 |

显式部署命令 `init` 使用同一会话锁协调建库与已有库验证，遵守[初始化入口](initialization.md#部署入口)的持有范围。上表及后文中的 `run` 初始化表示本次执行会话的启动准备，不包含创建第一份状态库。

两把锁采用 Linux `flock` 排他锁。接纳锁的操作均使用非阻塞方式，只有真正的锁冲突才表示已有持有者；其他错误单独报告。所有相关句柄关闭后内核释放锁，因此句柄必须由所属 CLI 进程独立持有，不得传给 `adb`、`ffmpeg` 等子进程。[Linux `flock` 文档](https://man7.org/linux/man-pages/man2/flock.2.html)

其他交接事务可观察到的阶段如下；取得和释放接纳锁的事务内部步骤按后文顺序执行。

| `run` 阶段 | 会话锁 | 接纳锁 | 提交能否据此认定存在接纳者 |
| --- | --- | --- | --- |
| 初始化，尚未获得普通执行资格 | 持有 | 未持有 | 否 |
| 接纳并处理工作 | 持有 | 持有 | 是 |
| 最终收尾 | 持有 | 已释放 | 否 |
| 受限收场或错误退出处理 | 持有至清理 | 不持有普通接纳资格 | 否 |
| 已结束且句柄已关闭 | 释放 | 释放 | 否 |

时钟异常期间，未填写 `scheduled_at` 的取消动作按[时钟异常期间的取消](clock-recovery.md#时钟异常期间的取消)执行。受限会话不持有普通接纳资格，不能据其可处理取消推断它能够承担普通调度工作。

正常 `run` 在短写事务内取得接纳锁，并在成功结束该事务后进入接纳阶段。该会话承诺检查状态库中的全部待处理工作，包括初始化期间已经提交的工作。数据库事务之外可以持续持有接纳锁；同一会话正常关闭接纳后直接完成收尾。

### 待处理工作的判定

`needs_run` 与正常退出检查都依据持久化工作和本地处理责任。下面的各类条件可以并存，任一类存在待处理责任即需要继续驱动；不能根据内存队列暂时为空或 `plan_seq` 未变化认定全部完成。

| 状态 | 是否构成待处理工作 |
| --- | --- |
| 尚未完成的动作，包括未来 `scheduled_at` 的 `pending` 动作 | 是 |
| 仍应继续执行或恢复的有限设备收场、产物处理或交付流程 | 是，由对应专题定义具体责任 |
| 应报告的业务变化尚未完成本地报告职责，包括受理拒绝及 ACK 校验错误 | 是 |
| 仅存在已结束动作留下的残留设备事实，且当前没有安全且必要的主动收场责任 | 否；该事实自身新增的报告责任另行判断 |
| 仅存在允许延后到后续正常会话处理的取回半成品清理残留 | 否；正常 `run` 按产物专题有限尝试，本次已结束的尝试不因重扫重启；新结果的报告责任另行判断 |
| 本地报告职责已完成，仅等待主程序发送或客户端 ACK | 否；补投按报告机会触发 |
| 工作是否完成无法可靠判断 | 不能判定为无工作，进入相应错误处理 |

报告责任的权威定义见[状态报告与累计确认](status-reports.md)。接管检查只读取持久化记录以及本次报告机会的处理结果；报告生成、文件操作和设备调用均在短写事务之外执行。重复提交、ACK 更新和受理拒绝也必须按实际产生或仍存在的责任判定，不能仅按“是否创建新 plan”判定。

取回半成品的查询或删除失败，在诊断可靠保存、其他职责及报告处理完成后允许正常收尾，具体见[半成品清理失败与后续处理](obtaining-outputs.md#半成品清理失败与后续处理)。`submit` 不执行该清理；仅剩允许延后处理的清理残留且无其他责任时，`needs_run` 为 `false`。状态库或必要报告职责无法完成仍按会话错误处理。

有效 ACK 的报告影响由[ACK 与业务变化](status-reports.md#ack-与业务变化)定义。若本次处理仅吸收有效 ACK，且处理后没有其他待处理责任，则 `needs_run: false`；若仍有其他责任，继续按接纳锁和工作状态判断。有效 ACK 本身不阻止 `run` 正常关闭接纳。

### `submit` 的事务顺序

1. 在事务外读取并解析计划文件；读取或解析失败时准备对应输入失败结果，成功时检查 `request_id`。依赖已受理请求、ACK 和其他持久化状态的判断留在事务内；事务内的已受理关联决定复用或首次受理，只有首次受理需要校验正文。
2. 开启短写事务，输入未能完整读取或解析时保存相应失败结果，其他输入按[计划正文与 ACK 独立处理](plan-acceptance.md#计划正文与-ack-独立处理)追加本次状态变化的历史并同步更新投影，再依据更新后的状态判定提交后是否存在待处理工作。输入读取失败、受理拒绝及 ACK 校验错误均属于应保存的业务结果；具体记录身份由报告专题定义。
3. 有待处理工作时，尝试以非阻塞方式取得接纳锁。如果成功，说明当前没有接纳者；立即释放本次探测取得的锁。如果发生锁冲突，说明有 `run` 持有接纳资格。
4. 保存本次 `needs_run` 判定并提交数据库事务。判定是本次响应数据，不要求新增持久化会话登记表。提交成功后才输出成功响应。

只有 `run` 可以在写事务结束后继续持有接纳锁。所有提交探测都受同一数据库写事务串行约束，且临时锁在事务结束前释放，所以一个 `submit` 不会把另一个 `submit` 的探测误认成接纳者。`submit` 不取得会话锁。

| 事务及工作判定 | 待处理工作 | 接纳锁观察 | 结果 |
| --- | --- | --- | --- |
| 可靠且提交成功 | 无 | 无需探测 | `needs_run: false` |
| 可靠且提交成功 | 有 | 锁冲突，存在接纳者 | `needs_run: false` |
| 可靠且提交成功 | 有 | 临时取得并成功释放，无接纳者 | `needs_run: true` |
| 状态判断、锁操作或事务提交失败，或结果不确定 | 任意 | 任意 | 会话错误，不输出成功的接管判定 |

接纳观察表达事务内该判定时点的责任。随后发生的进程启动、错误或突然终止，按对应生命周期规则处理。

### `run` 正常关闭接纳

1. 在接纳阶段完成已有动作及本次应执行的报告工作。设备操作、报告构建和文件发布不占用交接写事务。
2. 开启短写事务，基于当前持久化状态重新检查待处理工作。必须检查初始化或上次调度检查之后的提交，以及报告生成期间出现的业务变化。
3. 仍有待处理工作时，结束事务，保持接纳锁并继续处理工作；工作完成后再次检查。
4. 确认没有待处理工作时，在写事务结束前释放接纳锁。此后当前会话完成资源清理和最终结果输出，继续持有会话锁至退出清理。

正常释放接纳锁是关闭接纳的边界。释放之后即使数据库事务结束或资源清理失败，该会话也保持关闭接纳并按错误路径退出；下一次会话重新建立接纳资格。

`run` 先取得会话锁，再开启交接写事务；事务内只进行非阻塞接纳锁操作，不等待会话锁、设备操作或其他长任务。接纳锁正常取得、提交探测及正常释放都必须遵守同一事务顺序。

| 竞争顺序 | 当前会话行为 | 提交结果 |
| --- | --- | --- |
| 提交事务先结束，接纳者随后检查退出条件 | 看到待处理工作则继续；若已经处理完则可以正常收尾 | 有接纳者时返回 `false` |
| 关闭接纳的事务先结束，随后提交仍需驱动的工作 | 旧会话保持收尾 | 没有其他接纳者时返回 `true` |
| 多个 `submit` 并发 | 各写事务依次执行，探测期间互不误认 | 每次按自己的事务状态判定；多个 `true` 由主程序合并 |

### 失败与恢复边界

进程突然终止会释放其锁句柄；内核锁与数据库事务不组成跨资源的原子提交。上述顺序保证正常受理和正常收尾之间的交接，异常情况下以持久化提交结果和下一次实际启动的 `run` 进行恢复。

| 中断位置或错误 | 处理与恢复要求 |
| --- | --- |
| 提交事务尚未成功 | 不报告提交成功；事务回滚后的状态由后续请求按幂等规则重新判断 |
| 数据库提交结果不确定 | 报告会话错误；不得自行断言请求不存在，后续重试按持久化请求记录判定 |
| 提交成功，但进程在返回响应前终止 | 已提交的请求及业务结果保留，后续实际启动的 `run` 可以发现；客户端未获确认时按重送规则处理，已受理 ID 复用原计划 |
| 观察到接纳者后，该 `run` 崩溃或进入会话级错误退出 | 已提交工作保留；不承诺依赖该接纳者继续执行，等待后续实际启动的 `run` |
| `run` 关闭接纳后，在最终退出前终止 | 接纳资格保持关闭；后续提交按锁的实际状态判断 |
| 锁探测发生非冲突错误，或临时锁释放失败 | 保留诊断信息并终止成功判定；不得当作存在或不存在接纳者 |

异常退出清理可以释放接纳锁，即使数据库已无法开启事务。该路径表示会话失去履职能力，结果按会话错误处理；正常收尾仍必须经过事务内的最终工作检查。

主程序有限重启能力仍是待外部确认的建议，本协议不以该能力为前提。主机再次上电后的首次启动规则继续提供已确认的恢复入口。

## 新计划受理序列

名称固定为：`plan_seq` 表示每份新计划的受理序列，`latest_plan_seq` 表示库中最新受理序列，`last_seen_plan_seq` 表示当前 `run` 已吸收的序列。后者是可重建的会话内存状态；计划受理顺序以持久化的 `plan_seq` 为准。

首次成功受理新计划时分配单调递增的 `plan_seq`。同一 `request_id` 的幂等重试不推进该序列。状态库提供轻量读取的 `latest_plan_seq`。序列推进须与新计划成功受理保持一致。

该序列定义受理顺序；[调度与设备执行](scheduling-execution.md)据此发现新计划。业务变化报告使用的 `change_seq` 由[状态报告与累计确认](status-reports.md)定义。

## 持久化记录

以下是支撑本专题行为的逻辑记录建议；表划分和内部接口不构成已确认的实现约束。

请求/计划投影包含：`request_id`、`plan_instance_id`、`plan_seq`、首次成功受理的结构化计划字段及动作参数、`created_at`、`name`、计划状态。首次受理历史保存足以重建该计划语义的结构化数据；后续状态变化追加历史并同步更新投影。输入 JSON 文件由主程序管理，计划恢复和 JSON 重建依据数据库中的已受理数据。数据保留遵守[全局规则](system-foundations.md#持久化与配置基础)，计划完成后仍保留请求、计划、动作及其历史关联。

受理序列投影提供 `latest_plan_seq`。待处理工作依据对应业务投影和报告处理事实判定，实际接纳资格由本专题的内核锁协议表达，不能从历史回放中取得。

## 验收要求

### 单元测试

会话错误的责任分类、诊断及应急停止按[会话错误验收要求](session-errors.md#验收要求)验证；本节继续验证输入、接管与结果通道。

待处理工作判定覆盖仅有允许延后处理的半成品清理残留、另有新增诊断报告责任及另有未完成动作的组合。残留自身不要求立即再启动，新增报告或动作责任仍参与正常交接；本次清理尝试结束后不能因退出检查重新产生尝试责任。

隔离状态存储和会话协作者，验证公共输入、请求幂等判断、group 约束、取消寻址与进程响应的行为分支。

按[输入计划文件读取失败](cli-commands.md#输入计划文件读取失败)覆盖文件不存在、打开失败、读取中途失败、完整读取后解析失败和成功。即使失败前已读片段含有合法请求 ID 或 ACK，也不能用于受理或确认；验证失败阶段、路径及实际原因得以保留。分别覆盖失败结果保存成功、失败和提交结果未知，区分输入失败与会话错误；`submit` 的成功路径仍按报告责任及接纳者状态产生 `needs_run`。

首次受理的计划校验按[整份校验与原子受理](plan-acceptance.md#整份校验与原子受理)分类：覆盖公共结构和动作名称错误、未知类型、未支持类型，以及已支持动作自身校验失败。改变错误动作在数组中的位置，结果仍按错误范围决定；组合整份拒绝条件与动作自身错误时不注册任何实例。已有成功受理关联的请求以缺失、非数组或空数组的 `actions` 重送时仍复用原计划。断言拒绝结果或包含失败动作的完整受理内容，不复制内部校验调用顺序。

按 `group` 表覆盖动作类型与字段有无、合法性的组合。取回动作填写表示自身所属组的 `group` 时，仅该动作登记为 `failed`，包括空值及与来源组不同名的情况；该字段省略且来源引用合法时正常处理。错误输入保留供诊断，但取回动作不成为组成员。已受理 ID 的重试只复用原计划；组取消只选择实际组成员，不自动选择引用该组的取回动作。

其他支持的动作分别组合组名合法性与自身其他参数合法性：合法组名保持成员关系，其他参数错误只使动作失败；非法组名使该动作失败且没有有效组成员关系。录像因自身参数失败时仍按类型作为合法所属组的产物来源，取回执行时按终态无产物处理，不因录像已失败而排除它。

按[计划执行状态](plan-acceptance.md#计划执行状态)覆盖全部终态与是否曾开始执行的四种组合。验证部分动作未经执行就取消或过期、其余未开始时仍为 `pending`；曾执行的动作已结束而剩余动作等待未来时间时仍为 `running`；全部未经执行就终态时直接成为 `completed`。混合成功、失败、过期和取消均按全部终态判定完成，正在收场的非终态动作仍阻止完成；产物保留、交付及 ACK 不改变计划执行状态。历史不可可靠解释不能判为未开始或已完成。

首次受理分别覆盖全部动作合法、部分为校验失败的动作、全部为校验失败的动作。验证计划初始状态分别为 `pending`、`pending`、`completed`，均没有开始执行事实；失败动作尝试次数为零，不受未来 `scheduled_at` 或当前墙钟可信性影响。

按请求 ID 与[计划正文与 ACK 独立处理](plan-acceptance.md#计划正文与-ack-独立处理)的决策表推导用例；计划分别覆盖首次受理、幂等复用、请求 ID 错误和首次正文静态拒绝，ACK 分别覆盖有效推进、有效但不推进、无效和未提供。已受理 ID 的重试覆盖正文变化、字段缺省及非法动作参数，均复用原计划；原计划分别处于 `pending`、`running` 和 `completed`。另行覆盖整份 JSON 无法解析，验证不会受理计划或吸收 ACK。

`submit` 结束结果按会话完成情况与业务错误分别分类。正常完成时覆盖业务错误有无及 `needs_run` 的两个布尔值，验证响应携带实际接管判定；状态保存、锁操作失败及提交结果不确定时验证返回会话错误，不给出成功的接管判定。

结果序列化验证单行 JSON 和换行边界、协议字段及特殊字符转义；日志文案不作逐字断言。错误结果精确验证 `reason` 和 `details` 的结构与机器标识，覆盖空上下文及主程序未知的错误标识。最终结果判定按[stdout 与退出状态](cli-commands.md#stdout-与退出状态)决策表覆盖，包括 `submit` 成功消息缺少 `needs_run`，以及 `error` 配合退出码 `0`、`1` 和其他退出码的情况。

### 集成测试

通过真实 CLI 进程、配置文件及驱动定义验证 `describe` 的导出与重定向：设备未连接、没有业务状态库时仍可成功导出；输出只含完整能力说明 JSON，日志进入 stderr，退出码为 `0`，不创建状态库或交接文件。组合受接口约束的替身验证该入口不调用设备、状态库或会话锁协作者。配置或定义错误、生成失败、输出失败及异常终止按命令决策表验收，失败产物不能作为成功导出交付。

组合真实 SQLite、独立 `run/submit` 进程与主程序启动逻辑，验证并发受理、收尾交接、重复提交和 pending-start（待启动标记）。涉及进程生命周期的验证归集成测试。

通过真实子进程管道验证最终 JSON、stderr 诊断与退出状态的配合，覆盖成功、已处理的业务错误、会话级错误、输出截断和异常终止。正常完成时精确断言退出码 `0`，主动报告会话错误时精确断言退出码 `1`。分段读取完整消息仍应正确判定；日志及设备工具输出不得混入 stdout，收到消息后仍须观察进程终止状态。

使用已有持久化任务与错误的新计划文件组合运行 `camctl run <plan-path>`，验证本次输入失败时，已有任务仍按正常资格规则获得驱动，原请求记录保持不变。覆盖文件不存在、打开失败、JSON 格式错误及首次校验发现整份拒绝条件。另行覆盖新计划仅包含已支持动作自身错误，验证计划受理且错误动作失败。组合真实 SQLite、报告及进程响应，验证失败结果可靠保存、正常完成报告收尾后返回 `succeeded` 和退出码 `0`；报告或数据库职责失败则按会话错误处理。

通过 `submit` 输入不存在的文件，分别组合存在与不存在接纳者，验证错误报告责任参与 `needs_run` 判断，且不创建计划或推进 `plan_seq`。在失败结果提交前后中断，后续裸 `run` 仅从已提交历史恢复报告责任，不从路径或片段构造计划；客户端重送完整原请求后按实际受理关联首次注册或幂等复用。读取中途失败通过受真实读取接口约束的故障协作者与真实状态库组合验证，累计 ACK 保持不变。

使用真实 SQLite 验证首次受理时计划、全部动作、请求关联和受理序列的历史与投影一同提交。在部分历史追加或投影更新后中断或令事务失败，确认回滚后不存在部分注册结果；在提交后响应前中断，确认重试复用完整实例集合。两个计划输入入口均需覆盖。

通过两个计划输入入口提交空动作数组，验证保存整份拒绝结果，不注册计划或动作、不推进 `plan_seq`，有效 ACK 仍独立处理。对包含一个合法动作的计划，受理提交边界处计划与动作均为 `pending`；以同一请求 ID 携带空数组重送时仍保留原实例和实际执行状态。

两个入口分别提交合法动作与字段错误的已支持动作，覆盖录像缺少所选参数类型要求的字段、取回来源格式错误、清理目标列表为空、取消目标格式错误，以及各类型违反自身时间、策略或所属组要求的情况。验证完整计划一次受理，合法动作为 `pending`，错误动作为 `failed`，`plan_seq` 正常推进；报告关联错误动作及原因。报告动作提供非法的 `scheduled_at` 值时验证相同结果。

组合真实 SQLite、调度与受接口约束的设备和文件协作者，验证校验失败动作不启动或停止录像、不创建 delivery、不登记删除意图、不改变取消目标；错误的报告动作不执行显式报告请求，但会话仍发布受理及失败事实。同一计划里的合法动作正常获得调度。将参数错误的录像放入合法组并取回该组，验证来源仍包含该录像，执行时记录终态无产物失败。

组合真实 SQLite、报告和历史回放，分别在包含校验失败动作的受理事务提交前后中断，验证完整计划、失败结果和有效关联整体回滚或提交。用同一 ID 修正参数重送时保留原失败动作及输入；新 ID 可表达新的执行意图。重建计划 JSON 保留错误输入的结构化字段及值；全部动作在受理时失败的计划直接为 `completed`，仍产生受理和动作失败报告，ACK 独立处理。

分别回放受理前后及后续执行的已提交边界，验证完整计划与动作状态；重建当前投影应与正常提交得到的状态一致。组合幂等重试、ACK 和受理错误，验证历史顺序、`plan_seq` 与 `change_seq` 各自的职责。回放不能发送设备命令、执行文件操作、重新生成身份或取得会话接纳资格；历史缺失或不可可靠解释时须明确失败。

组合真实 SQLite、动作调度与报告，分别在首个动作开始及最后一个动作进入终态的事务提交前后中断，验证动作历史与计划投影整体提交或回滚。重启及同一请求重送后计划状态保持历史所定义的阶段；从较早报告边界重建时得到该时刻的计划状态，后续执行不能改写旧报告。计划阶段变化须随相应动作变化被报告覆盖，不能只更新当前计划投影而漏报。

受理结果提交后由主程序删除输入文件，验证当前会话执行和后续裸 `run` 恢复仍使用持久化计划；camctl 的执行、恢复和清理流程遵守输入文件归主程序管理的责任边界。

两个计划输入入口均验证正文与 ACK 的独立业务结果及共同持久化：计划拒绝时有效 ACK 仍被吸收，无效 ACK 不阻止合法计划受理，双重错误均有报告记录；幂等复用不增加实例或推进 `plan_seq`，本次正文不改变原计划内容与执行进度。事务回滚不留下部分更新，提交结果不确定时按数据库事实恢复。

使用不同正文并发提交同一 ID，验证只有首个成功提交的完整计划被保存，后续提交复用该实例。覆盖先前输入被拒绝、注册事务回滚及提交后响应丢失，再次提交该 ID 的情况；已受理 ID 在两个入口间重试也遵守同一规则。

组合客户端待确认记录、主程序中转与真实状态库，分别在首次受理提交前和提交后中断 CLI。原输入文件随后被主程序删除，客户端未获确认时用同一 ID 和原计划内容重送，验证前者可首次受理、后者复用原实例，最终由状态报告确认。收到已受理但动作未完成的报告后，不再因等待动作完成而判定受理未知。

带计划启动后令时钟检查失败，验证受理结果已经提交、普通动作尚未执行；下一次时钟正常的 `run` 能发现同一计划及动作实例。覆盖首次受理、幂等复用、业务拒绝，以及受理提交后、时钟检查前进程中断的情况。

会话交接使用真实 SQLite、多进程和内核锁验证。以可控的同步点安排“提交在退出检查之前”“关闭接纳在提交之前”和“多个提交探测”的顺序，断言工作由当前会话处理或得到 `needs_run: true`，不依赖随机 sleep 碰撞竞态。验证初始化、接纳、收尾各阶段的锁生命周期，以及设备工具子进程不继承锁句柄。

在提交前、提交后响应前、探测接纳后、关闭接纳后终止相关进程，验证已提交状态与后续恢复。另行验证报告准备期间提交仅产生受理拒绝等业务变化时，`plan_seq` 未推进也不能漏掉报告责任。锁错误与提交结果不确定的分类可用受接口约束的替身验证；真实锁竞争和事务提交恢复归集成测试。

### 行为覆盖清单

以下按行为组织，执行时按上述单元测试与集成测试边界归类。

#### 请求幂等

- 同 `request_id` 重复提交不重新执行；
- 同 ID 以首次成功受理的正文为准，后续正文被忽略，原计划及执行进度保持不变；
- 同 ID 允许更新 ACK；
- 新 ID + 相同动作仍执行。

#### 整份计划受理

- 按公共校验与动作类型的错误范围判断是否受理；
- 已支持动作自身的输入错误在受理时登记为该动作 `failed`，其他动作一同受理；存在整份拒绝条件时不注册本次实例，也不推进新计划受理序列；
- 计划、全部动作、校验失败结果和请求关联在同一事务中提交；
- 注册事务回滚不留下部分实例，提交结果不确定时按持久化状态对账；
- 受理拒绝结果通过状态报告表达，已有计划及动作保持不变；
- 受理成功后，动作执行结果仍各自独立。

#### run / submit

- 每次上电均由主程序发起一次 `run`，覆盖无数据输入、有输入但无执行计划、有执行计划三个分区；
- 有执行计划时主程序先持久化文件，再通过 `camctl run <plan-path>` 传入；
- 带计划启动时，输入文件读取、输入解析、请求 ID 或首次正文校验错误不阻断已有任务的恢复与执行；
- 新计划被拒绝的业务结果由 camctl 向客户端表达，主程序仅需处理会话控制结果与文件中转；
- 正常收尾的 `run` 返回 `succeeded`，覆盖无业务失败、新计划被拒绝、动作失败或过期等分区；
- `submit` 正常完成输入处理、持久化及接管判断后返回 `succeeded` 和明确的 `needs_run` 布尔值，覆盖计划拒绝、ACK 无效及二者同时发生；
- `submit` 仅产生业务错误时，错误记录持久化且报告责任得到接管判定；会话成功不依赖动作执行或报告文件发布完成；
- 会话级错误返回 `error`，即使会话中已有业务动作成功；时钟异常的有限收场完成后仍报告时钟异常；
- 进程突然终止时允许没有最终 stdout 消息，主程序通过进程终止事实发现异常；
- submit 不要求可信墙钟；
- 带计划 `run` 在取得会话锁、验证状态库后先完成受理，再检查墙钟；
- 时钟检查失败不撤销受理结果，且不会据异常墙钟把新受理动作判为过期；
- 受理阶段未校验的本机时间不能推进可信历史时间下界；
- `needs_run` 与旧 run 收尾竞态；
- 主程序 pending-start 模拟；
- `run` 可以跨整个主机供电窗口持续运行；
- 所有需要驱动的计划工作完成后 `run` 可以主动正常退出；
- 未来 `pending` action 阻止 `run` 因“当前无立即工作”而提前退出；
- 运行中的 `run` 持续吸收后续 `submit` 成功受理的新计划；
- 只剩 `possibly_recording` 等残留 `device_fact` 时允许正常退出。

#### group

- 多动作共享 group；
- 取回动作不属于任何组，错误填写所属组字段时在受理阶段登记为 `failed`；来源参数仍可引用组；
- group 取回；
- group 取消只影响成员，不自动取消引用该组的取回动作；
- group 不影响调度。

## 实现时细化

正文定义第一版的业务规则。本节列出接口、配置和联调中需要落实的细节；先按正常流程推进，涉及正文尚未确定的业务结果时，再明确对应选择。

公共输入以[执行计划输入契约](plan-input.md)为依据，实际代码须将公共校验与驱动参数校验接在同一受理流程中，保留已确认的错误范围与事务边界。会话错误标识、诊断及主程序处理由[会话错误与恢复边界](session-errors.md)定义；实际代码需将各入口和协作者异常映射到同一责任分类。

会话接管机制已由本专题定义。实施前还需结合各业务专题细化待处理工作查询、报告本地职责的完成证据、事务等待预算、输入计划文件在读取期间的内容与可用性保障、文件读取的有限等待和重试预算，以及客户端重试间隔、次数预算和停止条件；这些内容不能以测试中假定“有工作/无工作”代替实际端到端契约。

### 主程序有限重启建议

状态：待用户与主程序负责人沟通确认。该能力属于对接建议，当前规格不以其获得接受或已经实现为前提。

建议场景：主机仍有电，但 camctl 在返回最终会话结果前崩溃或被系统终止。

建议行为：主程序确认原进程已退出后，按固定间隔、有限次数重新启动裸 `camctl run`，使已持久化任务在当前供电窗口内获得恢复机会。重启次数和间隔建议可配置，具体数值及计数范围待双方确认。

| 运行条件 | 当前可以依赖的恢复入口 |
| --- | --- |
| 主机再次上电 | 主程序按[主机上电后的首次启动](cli-commands.md#主机上电后的首次启动)规则发起 `run` |
| 主机仍有电，camctl 非预期退出，有限重启能力尚未确认 | 保留持久化恢复依据，等待下一次实际发起的 `run`；不承诺同一供电窗口内自动恢复 |
| 主机仍有电，双方确认并实现有限重启能力 | 按约定的次数和间隔尝试启动裸 `run`；预算耗尽后的处理须另行约定 |

camctl 已明确返回 `error` 时，主程序按[主程序处理边界](session-errors.md#主程序处理边界)保留诊断及已有待启动职责。基于错误新增自动重启的策略与预算仍需双方另行确认。
